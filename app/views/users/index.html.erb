<h2>Listing current users</h2>
<table>
	<tr><th>Email</th><th>Admin?</th><th>Delete</th></tr>
	<%@users.each do |user|%>
	<tr><td><%=user.email%></td>
		<td><%= user.admin?%> </td>
		<td><a class="btn" href="/users/admin">Make Admin</a></td>
		<td><%= link_to 'Destroy', candidate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		</tr>
	<%end%>
	</table>